{
  "name": "api-client-adapter",
  "version": "0.0.1",
  "scripts": {
    "compile": "babel -d lib/ src/ && browserify lib/index.js -o dist/bundle.js",
    "prepublish": "npm run compile",
    "test": "npm run compile && mocha --compilers js:babel-core/register"
  },
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-core": "^6.14.0",
    "babel-plugin-syntax-object-rest-spread": "^6.13.0",
    "babel-plugin-transform-object-rest-spread": "^6.8.0",
    "babel-polyfill": "^6.13.0",
    "babel-preset-es2015": "^6.14.0",
    "babel-preset-stage-3": "^6.11.0",
    "browserify": "^13.1.0",
    "chai": "^3.5.0",
    "eslint": "^3.4.0",
    "install": "^0.8.1",
    "mocha": "^3.0.2",
    "npm": "^3.10.8",
    "sinon-es6": "0.0.3"
  },
  "dependencies": {
  }
}
